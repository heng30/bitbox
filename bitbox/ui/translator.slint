export struct Translation {
    new: string,
    edit: string,
    delete: string,
    cancel: string,
    ok: string,
    setting: string,
    about: string,
    help: string,
    setting-btn: string,
    about-btn: string,
    help-btn: string,
    name: string,
    warning: string,
    delete-or-not: string,
    reset-or-not: string,

    name-label: string,
    status-label: string,
    online: string,
    offline: string,
    busy: string,
    idle: string,

    file-name: string,
    file-size: string,
    file-status: string,
    file-download: string,

    error: string,
    undownload: string,
    downloading: string,
    downloaded: string,
    download-failed: string,

    setting-dialog-ui: string,
    setting-dialog-ui-font-size: string,
    setting-dialog-ui-font-family: string,
    setting-dialog-ui-win-WH: string,
    setting-dialog-ui-win-W: string,
    setting-dialog-ui-win-H: string,
    setting-dialog-ui-language: string,

    setting-dialog-chat: string,
    setting-dialog-chat-username: string,
    setting-dialog-chat-status: string,
    setting-dialog-chat-online: string,
    setting-dialog-chat-busy: string,
    setting-dialog-chat-idle: string,
    setting-dialog-chat-cache-clean: string,

    no-data: string,
    no-message: string,

    tip-copy: string,
    tip-delete: string,
    tip-send: string,
    tip-record: string,
    tip-stop-record: string,
    tip-play: string,
    tip-text2voice: string,
    tip-clear: string,
    tip-stop: string,
    tip-reset: string,
    tip-mute: string,
    tip-save: string,
    tip-save-as-session-screen: string,
    tip-show-session-screen: string,
    tip-edit-session-screen: string,
    tip-help: string,
    tip-about: string,
    tip-setting: string,
    tip-last-session-pos: string,
    tip-cur-session-pos: string,
    tip-edit: string,
    tip-up: string,
    tip-down: string,
    tip-show: string,
    tip-hide: string,
    tip-flush-session: string,
    tip-zoom-out: string,
    tip-zoom-in: string,
    tip-download: string,
    tip-send-image: string,
    tip-send-file: string,
}

export global Translator {
    public pure function translation(is-cn: bool) -> Translation {
        return {
            new: is-cn ? "新建" : "New",
            edit: is-cn ? "编辑" : "Edit",
            delete: is-cn ? "删除" : "Delete",
            cancel: is-cn ? "取消" : "Cancel",
            ok: is-cn ? "确定" : "Confirm",
            setting-btn: is-cn ? "设置" : "Setting",
            about-btn: is-cn ? "关于" : "About",
            help-btn: is-cn ? "帮助" : "Help",
            setting: is-cn ? "设     置" : "Setting",
            about: is-cn ?   "关     于" : "About",
            help: is-cn ?    "帮     助" : "Help",
            name: is-cn ?    "名     称" : "Name",
            warning: is-cn ? "警     告" : "Warning",
            delete-or-not: is-cn ? "是否删除？" : "Are You Sure to Delete?",
            reset-or-not: is-cn ? "是否重置？" : "Reset or not?",

            name-label: is-cn ? "昵称:" : "Name:",
            status-label: is-cn ? "状态:" : "Status:",
            online: is-cn ? "在线" : "Online",
            offline: is-cn ? "离线" : "Offline",
            busy: is-cn ? "正忙" : "Busy",
            idle: is-cn ? "空闲" : "Idle",

            file-name: is-cn ? "名称:" : "Name:",
            file-size: is-cn ? "大小:" : "Size:",
            file-status: is-cn ? "状态:" : "Status:",
            file-download: is-cn ? "下载": "Download",

            error: is-cn ? "出错" : "Error",
            undownload: is-cn ? "未下载" : "Undownload",
            downloading: is-cn ? "正在下载" : "Downloading",
            downloaded: is-cn ? "已下载" : "Downloaded",
            download-failed: is-cn ? "下载失败" : "Download Failed",

            setting-dialog-ui: is-cn ? "界面" : "UI",
            setting-dialog-ui-font-size: is-cn ? "字体大小:" : "Font Size:",
            setting-dialog-ui-font-family: is-cn ? "字体样式:" : "Font Family:",
            setting-dialog-ui-win-WH: is-cn ? "窗口大小:" : "Window Size:",
            setting-dialog-ui-win-W: is-cn ? "宽" : "Width",
            setting-dialog-ui-win-H: is-cn ?  "高" : "Height",
            setting-dialog-ui-language: is-cn ? "选择语言:" : "Language:",

            setting-dialog-chat: is-cn ? "会话" : "Chat",
            setting-dialog-chat-username: is-cn ? "昵称:" : "Nickname",
            setting-dialog-chat-status: is-cn ? "状态:" : "Status:",
            setting-dialog-chat-online: is-cn ? "在线" : "Online",
            setting-dialog-chat-busy: is-cn ? "正忙" : "Busy",
            setting-dialog-chat-idle: is-cn ? "空闲" : "Idle",
            setting-dialog-chat-cache-clean: is-cn ? "清除缓存" : "Clean Cache",

            no-data: is-cn ? "没有数据" : "No Data",
            no-message: is-cn ? "没有信息" : "No Message",

            tip-copy: is-cn ? "复制" : "Copy",
            tip-send: is-cn ? "发送" : "Send",
            tip-clear: is-cn ? "清除" : "Clear",
            tip-play: is-cn ? "播放" : "Play",
            tip-stop: is-cn ? "停止" : "Stop",
            tip-reset: is-cn ? "重置" : "Reset",
            tip-delete: is-cn ? "删除" : "Delete",
            tip-record: is-cn ? "录音" : "Record",
            tip-stop-record: is-cn ? "停止录音" : "Stop Record",
            tip-text2voice: is-cn ? "文字转声音" : "Text to Voice",
            tip-mute: is-cn ? "停止播放音频" : "Stop Playing Aduio",
            tip-save: is-cn ? "保存" : "Save",
            tip-save-as-session-screen: is-cn ? "保存为会话启动屏" : "Save as Session Launching Screen",
            tip-show-session-screen: is-cn ? "展示启动屏" : "Show Launching Screen",
            tip-edit-session-screen: is-cn ? "编辑启动屏" : "Edit Launching Screen",
            tip-help: is-cn ? "帮助" : "Help",
            tip-about: is-cn ? "关于" : "About",
            tip-setting: is-cn ? "设置" : "Setting",
            tip-last-session-pos: is-cn ? "上一次位置" : "Previous Position",
            tip-cur-session-pos: is-cn ? "当前位置" : "Current Position",
            tip-edit: is-cn ? "编辑" : "Edit",
            tip-up: is-cn ? "向上" : "Move Up",
            tip-down: is-cn ? "向下" : "Move down",
            tip-show: is-cn ? "显示" : "Show",
            tip-hide: is-cn ? "隐藏" : "Hide",
            tip-flush-session: is-cn ? "刷新列表" : "Flush session",
            tip-zoom-out: is-cn ? "缩小" : "Zoom Out",
            tip-zoom-in: is-cn ? "放大" : "Zoom In",
            tip-download: is-cn ? "下载" : "Download",
            tip-send-image: is-cn ? "发送图片" : "Send Image",
            tip-send-file: is-cn ? "发送文件" : "Send File",
        };
    }
}
